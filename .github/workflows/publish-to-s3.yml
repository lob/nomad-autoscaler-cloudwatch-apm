name: Publish to S3

on:
  workflow_dispatch:
    inputs:
      version:
        description: "GitHub Release tag to publish (e.g. v1.2.3)"
        required: true
        type: string

jobs:
  publish:
    runs-on: lob-runner
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download release binaries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download "${{ inputs.version }}" \
            --pattern "nomad-autoscaler-cloudwatch-apm_linux_*" \
            --pattern "nomad-autoscaler-cloudwatch-apm_darwin_*" \
            --dir ./dist

      - name: Upload to S3
        run: |
          for binary in \
            nomad-autoscaler-cloudwatch-apm_linux_amd64 \
            nomad-autoscaler-cloudwatch-apm_linux_arm64 \
            nomad-autoscaler-cloudwatch-apm_darwin_amd64 \
            nomad-autoscaler-cloudwatch-apm_darwin_arm64; do
            aws s3 cp "dist/${binary}" \
              "s3://lob-nomad-autoscaler-plugins/cloudwatch/${{ inputs.version }}/${binary}"
          done
